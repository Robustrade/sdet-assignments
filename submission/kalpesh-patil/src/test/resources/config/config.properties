# Test environment defaults. The wallet service fixture and embedded PostgreSQL
# are started in-process, so there is no host/credentials config here on purpose.
default.currency=AED
concurrency.request.timeout.seconds=10
